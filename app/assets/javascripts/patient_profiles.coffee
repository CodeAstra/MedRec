# # Place all the behaviors and hooks related to the matching controller here.
# # All this logic will automatically be available in application.js.
# # You can use CoffeeScript in this file: http://coffeescript.org/
# var name = current_user.name;
# var divide = name.split(",");
# var name = divide[0] + divide[n-1] ; 


# var dateOfBirth = patient_profile.dateofbirth;
# var birthDivide = dateofbirth.split(",");
# var dob =  birthDivide[0] + birthDivide[1];

# var totalCode = name + dob ;

